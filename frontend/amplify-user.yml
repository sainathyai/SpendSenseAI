version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm ci
    build:
      commands:
        - npm run build:user
  artifacts:
    baseDirectory: frontend/dist-user
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*




